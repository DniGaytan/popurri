module arrays_demo

class myClass {
    var array : [int] [5]
}

var obj : myClass, tmp : int

for i in obj.array {
    print('Input value #', i + 1, ':')
    input(tmp)
    obj.array[i] = tmp
}

for i in obj.array {
    print(obj.array[i])
}